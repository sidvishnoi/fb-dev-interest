get api key
send api requests 
    request one group at a time
    filter by hashtags
    cache results
show in feed
    check if fb:view-source can give something

readme, presentation, video and stuff
